services:
  - type: web
    name: flowtrace-ai-backend
    env: node
    plan: free
    autoDeploy: true
    rootDir: backend
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /health
