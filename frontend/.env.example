# Production (Vercel): set in Project → Environment Variables → Production.
# Use your Railway HTTPS URL, no trailing slash.
# Example: VITE_API_URL=https://your-service.up.railway.app
#
# Vite bakes this in at build time — redeploy after changing.
VITE_API_URL=
